<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Details</title>
    <style>
        p{
            font-size: 1.3em;
        }
    </style>
</head>
<body>
    <div class="jumbotron text-center">
        <h1>Book Information</h1>
    </div>
    <div class="row">
        <p>Book ID:  <% if (typeof (item.bookID) !== "undefined") { %><%= item.bookID %><% } %></p><br>
        <p>Book Name:  <% if (typeof (item.name) !== "undefined") { %><%= item.name %><% } %></p><br>
        <p>Author:  <% if (typeof (item.author) !== "undefined") { %><%= item.author %><% } %></p><br>
        <p>Publisher:  <% if (typeof (item.publisher) !== "undefined") { %><%= item.publisher %><% } %></p><br>
        <p>ISBN:  <% if (typeof (item.ISBN) !== "undefined") { %><%= item.ISBN %><% } %></p><br>
        <p>Genre:  <% if (typeof (item.genre) !== "undefined") { %><%= item.genre %><% } %></p><br>
        <p>Description:  <% if (typeof (item.description) !== "undefined") { %><%= item.description %><% } %></p><br>
    </div>
        <a href="/edit?_id=<%= item._id %>" >Update the information</a><br><br>
        <a href="/delete?_id=<%= item._id %>&owner=<%= item.ownerID %>">Delete the information</a><br><br>
        <a href="/home">Home</a>
</body>
</html>